image:
  tag: "2.0.3"

config:
  ## https://docs.fluentbit.io/manual/pipeline/outputs
  # https://docs.fluentbit.io/manual/pipeline/outputs/elasticsearch
  outputs: |
    [OUTPUT]
        Name es
        Match kube.*
        Host elasticsearch-master-es-default
        Logstash_Format On
        Retry_Limit False
        tls On
        tls.verify Off
        HTTP_User elastic
        HTTP_Passwd xc8D3r3JZ3p4o96dl2wMD01A
        Suppress_Type_Name On
        Trace_Error On
        Buffer_Size 1Mb
        Replace_Dots On

    [OUTPUT]
        Name es
        Match host.*
        Host elasticsearch-master-es-default
        Logstash_Format On
        Logstash_Prefix node
        Retry_Limit False
        tls On
        tls.verify Off
        HTTP_User elastic
        HTTP_Passwd xc8D3r3JZ3p4o96dl2wMD01A
        Suppress_Type_Name On
        Trace_Error On
        Buffer_Size 1Mb
        Replace_Dots On